export const invalidTypeError = (message = '') => {
  throw `invalid type error! ${message}`;
}
export const illegalArgumentsError = (message = '') => {
  throw `illegal arguments error! ${message}`;
}
